server:
  port: 8761
eureka:
  client:
    register-with-eureka: false #是否要注册eureka server
    fetch-registry: false #是否要同步其他的eureka server的数据
    service-url:
      defaultZone: http://localhost:8761/eureka/