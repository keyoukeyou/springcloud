server:
  port: 8010
spring:
  application:
    name: provider  #当前服务注册在Eureka Server的名字
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true #注是否将当前服务的IP注册到Eureka Server